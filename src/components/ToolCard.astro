---
import type { ToolDefinition } from '../data/tools';

const { tool, compact = false } = Astro.props as { tool: ToolDefinition; compact?: boolean };
---

<a
	href={tool.path}
	class:list={[
		'group relative block overflow-hidden rounded-[1.8rem] border border-slate-200 bg-white transition duration-200 hover:-translate-y-0.5 hover:border-slate-300 hover:shadow-[0_18px_60px_-45px_rgba(15,23,42,0.45)]',
		compact ? 'block p-5' : 'block p-6',
	]}
>
	<div class="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-slate-200 to-transparent opacity-0 transition duration-200 group-hover:opacity-100"></div>
	<div class="flex items-start justify-between gap-4">
		<div>
			<p class="text-xs font-semibold uppercase tracking-[0.22em] text-blue-600">{tool.categoryLabel}</p>
			<h3 class="mt-3 text-xl font-bold text-slate-900">{tool.name}</h3>
		</div>
		<span class="rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.18em] text-slate-500">/{tool.slug}</span>
	</div>
	<p class:list={[compact ? 'mt-3 text-sm leading-6' : 'mt-4 text-sm leading-7', 'text-slate-600']}>
		{compact ? tool.cardDescription : tool.metaDescription}
	</p>
	{!compact && <p class="mt-5 text-xs font-semibold uppercase tracking-[0.22em] text-slate-400 transition group-hover:text-slate-700">Open tool</p>}
</a>
